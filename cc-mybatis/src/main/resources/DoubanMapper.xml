<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mybatis.douban.mapper.IDoubanMapper">

    <select id="getCommentByPageLimitBookId" resultType="Alias_Comment" useCache="false">
        select * from ccl.t_amount
        limit 3;
    </select>

</mapper>